node_modules/
artifacts/
cache/
coverage/
coverage.json
typechain-types/
.env
app/deployments.json

# Scoped-RPC per-user tokens (secrets)
app/rpc-tokens.json
app/rpc-revoked.json

# Pending KYC requests (names people typed — not for the repo)
app/kyc-requests.json

# Pending multisig requests (labels people typed, same reason)
app/safe-requests.json

# operator-local overrides (may contain secrets)
ops/*.local.sh

# Generated paper builds (PDF/markdown deliverables, rebuilt on demand)
paper-out/
